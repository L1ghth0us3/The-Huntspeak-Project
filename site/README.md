# Huntspeak site

Public learning site for the Huntspeak project — Astro static site deployed to GitHub Pages.

## Develop

```bash
cd site
npm install
npm run dev
```

## Build

```bash
npm run build
```

Output lands in `dist/`. Content is read from the parent repo’s `docs/`, `lexicon/`, and `texts/` at build time.
